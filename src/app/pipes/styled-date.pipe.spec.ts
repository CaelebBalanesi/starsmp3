import { StyledDatePipe } from './styled-date.pipe';

describe('StyledDatePipe', () => {
  it('create an instance', () => {
    const pipe = new StyledDatePipe();
    expect(pipe).toBeTruthy();
  });
});
